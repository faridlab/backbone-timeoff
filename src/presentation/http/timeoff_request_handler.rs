//! TimeoffRequest REST handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Uses Axum and backbone-core's BackboneCrudHandler for all 12 CRUD endpoints.

use std::collections::HashMap;
use std::sync::Arc;

use axum::Router;
use serde::{Deserialize, Serialize};
use uuid::Uuid;
use chrono::{NaiveDate};

// Backbone framework imports
use backbone_core::http::BackboneCrudHandler;

// Auth integration (optional)
#[cfg(feature = "auth")]
use backbone_auth::middleware::AuthContext;
#[cfg(feature = "auth")]
use backbone_auth::AuthMiddleware;

// Domain imports
use crate::domain::entity::*;
use crate::application::service::{TimeoffRequestService, ServiceError};

// DTO imports
use crate::presentation::dto::{CreateTimeoffRequestDto, UpdateTimeoffRequestDto, PatchTimeoffRequestDto, TimeoffRequestResponseDto};


/// Application error type
#[derive(Debug, thiserror::Error)]
pub enum TimeoffRequestError {
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Validation error: {0}")]
    Validation(String),
    #[error("Database error: {0}")]
    Database(String),
    #[error("Internal error: {0}")]
    Internal(String),
}

impl From<ServiceError> for TimeoffRequestError {
    fn from(err: ServiceError) -> Self {
        match err {
            ServiceError::NotFound => Self::NotFound(err.to_string()),
            ServiceError::Validation(ref msg) => Self::Validation(msg.clone()),
            ServiceError::AlreadyExists(ref msg) => Self::Validation(msg.clone()),
            ServiceError::Repository(ref e) => Self::Database(e.to_string()),
            ServiceError::Internal(ref msg) => Self::Internal(msg.clone()),
        }
    }
}

impl axum::response::IntoResponse for TimeoffRequestError {
    fn into_response(self) -> axum::response::Response {
        use axum::http::StatusCode;
        use axum::Json;

        let (status, code) = match &self {
            Self::NotFound(_) => (StatusCode::NOT_FOUND, "TIMEOFFREQUEST_NOT_FOUND"),
            Self::Validation(_) => (StatusCode::BAD_REQUEST, "TIMEOFFREQUEST_VALIDATION_ERROR"),
            Self::Database(_) => (StatusCode::INTERNAL_SERVER_ERROR, "TIMEOFFREQUEST_DATABASE_ERROR"),
            Self::Internal(_) => (StatusCode::INTERNAL_SERVER_ERROR, "TIMEOFFREQUEST_INTERNAL_ERROR"),
        };

        let body = serde_json::json!({
            "success": false,
            "error": code,
            "message": self.to_string(),
        });

        (status, Json(body)).into_response()
    }
}

// =============================================================================
// Route Configuration
// =============================================================================

/// Create Axum router with all 16 Backbone endpoints for TimeoffRequest.
///
/// # Routes
///
/// | Method | Path | Description |
/// |--------|------|-------------|
/// | GET | /timeoff_requests | List with pagination |
/// | POST | /timeoff_requests | Create new |
/// | GET | /timeoff_requests/:id | Get by ID |
/// | PUT | /timeoff_requests/:id | Full update |
/// | PATCH | /timeoff_requests/:id | Partial update |
/// | DELETE | /timeoff_requests/:id | Soft delete |
/// | POST | /timeoff_requests/bulk | Bulk create |
/// | POST | /timeoff_requests/upsert | Upsert |
/// | GET | /timeoff_requests/trash | List deleted |
/// | POST | /timeoff_requests/:id/restore | Restore |
/// | DELETE | /timeoff_requests/empty | Empty trash |
/// | GET | /timeoff_requests/:id/deleted | Get deleted by ID |
/// | DELETE | /timeoff_requests/trash/:id | Permanent delete from trash |
/// | GET | /timeoff_requests/count | Count active entities |
/// | GET | /timeoff_requests/trash/count | Count deleted entities |
///
/// # Example
///
/// ```text
/// let service = Arc::new(TimeoffRequestService::with_repository(repository));
/// let router = create_timeoff_request_routes(service);
/// ```
pub fn create_timeoff_request_routes(service: Arc<TimeoffRequestService>) -> Router {
    BackboneCrudHandler::<TimeoffRequestService, TimeoffRequest, CreateTimeoffRequestDto, UpdateTimeoffRequestDto, TimeoffRequestResponseDto>::routes(
        service,
        "/timeoff_requests",
    )
}

/// Create Axum router with only the read (GET) endpoints for TimeoffRequest.
///
/// Safe for public, unauthenticated exposure (e.g., reference data).
/// Mutations must be served separately via `create_timeoff_request_write_routes`,
/// typically wrapped in an auth middleware layer.
pub fn create_timeoff_request_read_routes(service: Arc<TimeoffRequestService>) -> Router {
    BackboneCrudHandler::<TimeoffRequestService, TimeoffRequest, CreateTimeoffRequestDto, UpdateTimeoffRequestDto, TimeoffRequestResponseDto>::read_routes(
        service,
        "/timeoff_requests",
    )
}

/// Create Axum router with only the write (mutation) endpoints for TimeoffRequest.
///
/// These routes must NOT be publicly exposed. Wrap them with an auth
/// middleware before nesting into the application router.
///
/// # This is unguarded generic CRUD, not a validated write path
///
/// These are plain create/update/patch/delete mutations over the entity row —
/// they bypass all business invariants. If the module exposes a validated write
/// service (e.g. a command router over its domain engine), serve THAT instead
/// for any mutation that must respect domain rules.
pub fn create_timeoff_request_write_routes(service: Arc<TimeoffRequestService>) -> Router {
    BackboneCrudHandler::<TimeoffRequestService, TimeoffRequest, CreateTimeoffRequestDto, UpdateTimeoffRequestDto, TimeoffRequestResponseDto>::write_routes(
        service,
        "/timeoff_requests",
    )
}

/// Create authenticated routes with auth middleware.
///
/// Requires the `auth` feature flag. The `AuthMiddleware` implementation
/// is responsible for extracting and validating tokens, then providing
/// an `AuthContext` via request extensions.
#[cfg(feature = "auth")]
pub fn create_protected_timeoff_request_routes<A: AuthMiddleware + Send + Sync + 'static>(
    service: Arc<TimeoffRequestService>,
    auth: Arc<A>,
) -> Router {
    use axum::middleware;
    use axum::response::IntoResponse;

    let auth_layer = auth.clone();
    create_timeoff_request_routes(service)
        .layer(middleware::from_fn(move |mut req: axum::extract::Request, next: axum::middleware::Next| {
            let auth = auth_layer.clone();
            async move {
                let token = req.headers()
                    .get(axum::http::header::AUTHORIZATION)
                    .and_then(|h| h.to_str().ok())
                    .and_then(|raw| raw.strip_prefix("Bearer ").or_else(|| raw.strip_prefix("bearer ")))
                    .unwrap_or("");
                match auth.authenticate(token).await {
                    Ok(ctx) => {
                        req.extensions_mut().insert(ctx);
                        next.run(req).await
                    }
                    Err(_) => {
                        (axum::http::StatusCode::UNAUTHORIZED,
                         axum::Json(serde_json::json!({
                             "success": false,
                             "error": "unauthorized",
                             "message": "Authentication required"
                         }))
                        ).into_response()
                    }
                }
            }
        }))
}
